FROM golang:alpine AS builder

COPY . /app
WORKDIR /app
ENV CGO_ENABLED=0
RUN go build -o /dist/enricher cmd/enricher/main.go

FROM scratch

COPY --from=builder /dist/enricher /enricher
ENTRYPOINT ["/enricher"]
